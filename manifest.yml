applications:
- name: hackaton
  memory: 128M
  instances: 1
  services:
    messages: rabbitmq3
  timeout: 30
  stackato:
    description: HPE Hackaton - Dashboard
    # hooks:
      # pre-push:
      # - npm run dist
    ignores:
      - .babelrc
      - .editorconfig
      - .eslintignore
      - .eslintrc
      - .git
      - .sass-lint.yml
      - README.md
      - __tests__
      - public
      - server
      - src
      - node_modules
      - package.json.orig
      - webpack.config.babel.js
